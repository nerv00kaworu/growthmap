const test=require('node:test'),assert=require('node:assert/strict'),{registerCredentialIpc}=require('../credential-ipc');
test('credential IPC follows rotation and rollback through live store getter',async()=>{const handlers={},ipcMain={handle:(n,f)=>handlers[n]=f},calls=[],store=n=>({has:id=>(calls.push([n,'has',id]),n==='new'),set:(...a)=>calls.push([n,'set',...a]),delete:(...a)=>calls.push([n,'delete',...a]),recover:(...a)=>calls.push([n,'recover',...a])});let current=store('old');registerCredentialIpc({ipcMain,guard:()=>{},getStore:()=>current});await handlers['secrets:set']({},'p','value');current=store('new');assert.equal(await handlers['secrets:has']({},'p'),true);await handlers['secrets:delete']({},'p');current=store('old');await handlers['secrets:recover']({},'p',2,'set','value');assert.deepEqual(calls,[['old','set','p','value'],['new','has','p'],['new','delete','p'],['old','recover','p',2,'set','value']])});
