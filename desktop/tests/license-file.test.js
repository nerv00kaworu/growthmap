'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),os=require('node:os'),path=require('node:path');const {readStableRegularFile,samePath,windowsStable}=require('../license-file');
test('stable license reader rejects symlink hardlink and oversize',()=>{const d=fs.mkdtempSync(path.join(os.tmpdir(),'gm-license-')),p=path.join(d,'x');fs.writeFileSync(p,'{}');assert.equal(readStableRegularFile(p).toString(),'{}');const l=path.join(d,'l');fs.symlinkSync(p,l);assert.throws(()=>readStableRegularFile(l));const h=path.join(d,'h');fs.linkSync(p,h);if(process.platform!=='win32')assert.throws(()=>readStableRegularFile(p));fs.unlinkSync(h);fs.writeFileSync(p,Buffer.alloc(65537));assert.throws(()=>readStableRegularFile(p));});
test('reader uses same fd despite path swap after open',()=>{const d=fs.mkdtempSync(path.join(os.tmpdir(),'gm-license-')),p=path.join(d,'x'),evil=path.join(d,'evil');fs.writeFileSync(p,'good');fs.writeFileSync(evil,'different-size');const fake={...fs,openSync(file,flags){const fd=fs.openSync(file,flags);fs.renameSync(evil,p);return fd;}};assert.throws(()=>readStableRegularFile(p,{fsImpl:fake}),/unsafe|unstable/);});
test('bounded reader rejects short read',()=>{const d=fs.mkdtempSync(path.join(os.tmpdir(),'gm-short-')),p=path.join(d,'x');fs.writeFileSync(p,'abcdef');const fake={...fs,readSync(){return 0;}};assert.throws(()=>readStableRegularFile(p,{fsImpl:fake}),/short read/);});
test('Windows canonical path identity ignores case and separator spelling only',()=>{assert.equal(samePath('C:\\Users\\Runner\\File','c:/users/runner/file',true),true);assert.equal(samePath('C:\\Users\\Runner\\File','c:/users/runner/other',true),false);});
test('Windows stable metadata tolerates unavailable inode identity but rejects replacement',()=>{const base={size:4,mtimeMs:10,birthtimeMs:5,dev:0,ino:0};assert.equal(windowsStable(base,{...base,dev:12,ino:34}),true);assert.equal(windowsStable(base,{...base,size:5}),false);assert.equal(windowsStable({...base,dev:1,ino:2},{...base,dev:1,ino:3}),false);});
