'use strict';
const test=require('node:test'),assert=require('node:assert/strict');const {reloadRendererAfterRestart}=require('../sidecar-reload');
test('reload after ordinary restart uses new origin for renderer and health and requires UI marker',async()=>{let origin='http://127.0.0.1:4200',loaded,healthOrigin,probed=0;const window={isDestroyed:()=>false,loadURL:async url=>loaded=url,webContents:{executeJavaScript:async script=>{probed++;assert.match(script,/growthmap-title/);return true}}};assert.equal(await reloadRendererAfterRestart({getBaseUrl:()=>origin,window,health:async url=>healthOrigin=url}),true);assert.equal(loaded,origin);assert.equal(healthOrigin,origin);assert.equal(probed,1);assert.notEqual(loaded,'http://127.0.0.1:4100')});
test('reload rejects missing marker after health',async()=>{const window={isDestroyed:()=>false,loadURL:async()=>{},webContents:{executeJavaScript:async()=>false}};await assert.rejects(reloadRendererAfterRestart({getBaseUrl:()=> 'http://127.0.0.1:4200',window,health:async()=>{}}),/Required UI markers missing/)});
