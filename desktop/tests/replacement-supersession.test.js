const {createHostNeutralTestFiles}=require('./helpers/host-neutral-test-files');
const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),os=require('node:os'),path=require('node:path'),crypto=require('node:crypto'),{createReplacementJournal}=require('../replacement-journal'),{recoverReplacement}=require('../replacement-recovery');
const crypt={encrypt:s=>Buffer.from(s).toString('base64'),decrypt:b=>Buffer.from(String(b),'base64').toString()},ev=b=>({sha256:crypto.createHash('sha256').update(b).digest('hex'),size:b.length}),a={schemaVersion:1,bindings:{}},ids=['11111111-1111-4111-8111-111111111111','22222222-2222-4222-8222-222222222222'];
const begin=(j,db,id,old,next)=>j.begin({transitionId:id,mode:'foreign-import',databasePath:db,oldEvidence:ev(Buffer.from(old)),newEvidence:ev(Buffer.from(next)),authorityPre:a});
test('consumed terminal record permits a new replacement only after exact recovery verification',async()=>{const root=fs.mkdtempSync(path.join(os.tmpdir(),'gm-super-')),db=path.join(root,'growthmap.db'),j=createReplacementJournal({platform:'linux',userData:root,files:createHostNeutralTestFiles(path.join(root,'secrets')),...crypt});fs.writeFileSync(db,'NEW1');let first=begin(j,db,ids[0],'OLD','NEW1');first=j.update(first,{authorityNew:a,phase:'committed'});fs.writeFileSync(first.oldPath,'OLD');const freshBefore=createReplacementJournal({platform:'linux',userData:root,files:createHostNeutralTestFiles(path.join(root,'secrets')),...crypt});assert.throws(()=>begin(freshBefore,db,ids[1],'NEW1','NEW2'),/already pending/);await recoverReplacement({platform:'linux',journal:j,authority:{matches:()=>true,restore(){}},validate:async p=>ev(fs.readFileSync(p)),databasePath:db,fsyncDir:async()=>{}});assert.equal(j.read(),null);const fresh=createReplacementJournal({platform:'linux',userData:root,files:createHostNeutralTestFiles(path.join(root,'secrets')),...crypt}),second=begin(fresh,db,ids[1],'NEW1','NEW2');assert.equal(second.transitionId,ids[1]);assert.equal(fresh.read().phase,'intent')});
test('tampered pending terminal live forbids verification and preserves journal bytes',async()=>{const root=fs.mkdtempSync(path.join(os.tmpdir(),'gm-super-tamper-')),db=path.join(root,'growthmap.db'),j=createReplacementJournal({platform:'linux',userData:root,files:createHostNeutralTestFiles(path.join(root,'secrets')),...crypt});fs.writeFileSync(db,'NEW1');let first=begin(j,db,ids[0],'OLD','NEW1');first=j.update(first,{authorityNew:a,phase:'committed'});fs.writeFileSync(first.oldPath,'OLD');fs.writeFileSync(db,'EVIL');const file=path.join(root,'secrets','replacement-intent.bin'),bytes=fs.readFileSync(file),fresh=createReplacementJournal({platform:'linux',userData:root,files:createHostNeutralTestFiles(path.join(root,'secrets')),...crypt});await assert.rejects(recoverReplacement({platform:'linux',journal:fresh,authority:{matches:()=>true,restore(){}},validate:async p=>ev(fs.readFileSync(p)),databasePath:db,fsyncDir:async()=>{}}));assert.throws(()=>begin(fresh,db,ids[1],'NEW1','NEW2'));assert.deepEqual(fs.readFileSync(file),bytes)});
