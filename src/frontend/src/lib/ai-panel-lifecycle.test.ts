import test from"node:test";import assert from"node:assert/strict";import{mountAIPanelLifecycle}from"./ai-panel-lifecycle";import{createAIPanelController}from"./ai-panel-controller";import type{ProviderConfig}from"./types";
class T{m=new Map<string,Set<()=>void>>();addEventListener(e:string,f:()=>void){if(!this.m.has(e))this.m.set(e,new Set);this.m.get(e)!.add(f)}removeEventListener(e:string,f:()=>void){this.m.get(e)?.delete(f)}count(){return[...this.m.values()].reduce((n,s)=>n+s.size,0)}emit(e:string){this.m.get(e)?.forEach(f=>f())}}
const deferred=<T>()=>{let resolve!:(v:T)=>void,reject!:(e:unknown)=>void;const promise=new Promise<T>((a,b)=>{resolve=a;reject=b});return{promise,resolve,reject}};
const p:ProviderConfig={id:"p",name:"P",provider_type:"mock",endpoint:"",auth_type:"env",secret_env_key:"GROWTHMAP_LLM_KEY_P",model_name:"m",capabilities:[],cost_level:"none",enabled:true,created_at:"",updated_at:"",revision:1,secret_change_pending:false,credential_status:"ready"};
test("production adapter plus real controller fences strict replay deferred publication",async()=>{const lists:ReturnType<typeof deferred<ProviderConfig[]>>[]=[];const c=createAIPanelController({listProviders:()=>{const d=deferred<ProviderConfig[]>();lists.push(d);return d.promise},updateModel:async()=>p,testConnection:async()=>({}),confirm:()=>true,copy:async()=>{},now:()=>0,locale:()=>"en",currentIdentity:()=>null,onSaved:()=>{},onInvalidate:()=>{}}),t=new T();const mount=()=>mountAIPanelLifecycle({controller:c,target:t as never,loadProfiles:()=>{void c.list()},refresh:()=>{void c.list()}});const clean1=mount();clean1();const clean2=mount();lists[0].resolve([p]);await lists[0].promise;await Promise.resolve();assert.equal(c.getSnapshot().profiles.length,0);lists[1].resolve([p]);await lists[1].promise;await Promise.resolve();assert.equal(c.getSnapshot().profiles.length,1);t.emit("storage");assert.equal(lists.length,3);clean2();lists[2].resolve([]);await lists[2].promise;await Promise.resolve();assert.equal(c.getSnapshot().profiles.length,1);assert.equal(t.count(),0)});
