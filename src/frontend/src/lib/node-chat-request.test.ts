import test from"node:test";import assert from"node:assert/strict";import{requestNodeChat}from"./node-chat-request";import type{ChatOwner}from"./node-chat-controller";
test("exact production request adapter consumes the immutable captured owner tuple",async()=>{const owner:ChatOwner={generation:7,nodeId:"node-a",profile:{providerId:"profile-a",provider:"mock",model:"m",revision:9,selectionRevision:4}},calls:unknown[][]=[];const result=await requestNodeChat(owner,"hello",[{role:"user",content:"old"}],async(...args)=>{calls.push(args);return"reply"});assert.equal(result,"reply");assert.deepEqual(calls,[["node-a","hello",[{role:"user",content:"old"}],"profile-a",9,4]])});
test("production request adapter fails closed without captured profile",async()=>{const owner:ChatOwner={generation:1,nodeId:"n",profile:null};await assert.rejects(requestNodeChat(owner,"x",[],async()=>"bad"),/LOCAL_PROFILE_UNAVAILABLE/)});
