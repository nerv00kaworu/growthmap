import type {ProofSection} from '../content/proof-content';
export function ProofSections({content,variant='proof'}:{content:ProofSection;variant?:'proof'|'case'|'status'|'security'}){return <section className={`page proof-sections proof-${variant}`}>{content.eyebrow&&<p className="eyebrow">{content.eyebrow}</p>}{content.title&&<h2>{content.title}</h2>}{content.lead&&<p className="lead">{content.lead}</p>}{content.sections.map((section,index)=><section className="proof-group" key={section.title}><h3>{String(index+1).padStart(2,'0')} / {section.title}</h3><div className="proof-grid">{section.items.map(item=><article key={item.title}>{item.status&&<span className={`status-badge status-${item.status.toLowerCase().replaceAll(' ','-')}`}>{item.status}</span>}<h4>{item.title}</h4><p>{item.body}</p>{item.evidence&&<small>{item.evidence}</small>}</article>)}</div></section>)}</section>}
