import Link from 'next/link'; export function Page({eyebrow,title,children}:{eyebrow:string;title:string;children:React.ReactNode}){return <section className="page"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><div className="prose">{children}</div></section>} export const CTA=({href,children}:{href:string;children:React.ReactNode})=><Link className="button" href={href}>{children}</Link>;
