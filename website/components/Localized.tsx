import Link from 'next/link'; import {core,labels,type Locale} from '../content/i18n'; import {homeProof} from '../content/proof-content'; import {MapArt} from './Layout'; import {Detailed} from './ProductContent'; import {ProofSections} from './ProofSections';
const href=(l:Locale,p:string)=>`/${l}${p==='/'?'':p}`;
export function Home({locale}:{locale:Locale}){return <><section className="hero"><div><p className="eyebrow">{core[locale].home.eyebrow}</p><h1>{core[locale].home.title}</h1><p className="lead">{core[locale].home.lead}</p><div className="actions"><Link className="button" href={href(locale,'/features')}>{labels[locale].cta}</Link><Link className="textlink" href={href(locale,'/agents')}>{core[locale].home.map} →</Link><Link className="textlink" href={href(locale,'/docs')}>{locale==='zh-TW'?'查看操作文件':locale==='zh-CN'?'查看操作文档':'Read the docs'} →</Link></div></div><MapArt/></section><ProofSections content={homeProof[locale]}/><Detailed locale={locale} page="home" continuation/></>}
export function Product({locale}:{locale:Locale}){return <Detailed locale={locale} page="product"/>}
export function Download({locale}:{locale:Locale}){const copy=locale==='zh-TW'?{eyebrow:'下載',title:'下載 GrowthMap',lead:'目前提供 Windows 桌面版。Windows x64 版本正在準備發布；安裝與版本資訊會在可取得時更新。'}:locale==='zh-CN'?{eyebrow:'下载',title:'下载 GrowthMap',lead:'目前提供 Windows 桌面版。Windows x64 版本正在准备发布；安装与版本信息将在可获取时更新。'}:{eyebrow:'DOWNLOAD',title:'Download GrowthMap',lead:'GrowthMap is currently provided as a Windows desktop application. The Windows x64 release is being prepared; version and installation status will be updated when it is available.'};return <section className="page"><p className="eyebrow">{copy.eyebrow}</p><h1>{copy.title}</h1><p className="lead">{copy.lead}</p></section>}
export function Pricing({locale}:{locale:Locale}){const c=core[locale].pricing;return <section className="page"><p className="eyebrow">{c.eyebrow}</p><h1>{c.title}</h1><div className="price"><p>{c.first}</p><strong>US$10</strong><p>{c.after}</p></div><ul>{c.terms.map((x:string)=><li key={x}>{x}</li>)}</ul><p className="notice">{c.notice}</p><Link className="button" href={href(locale,'/buy')}>{c.demo}</Link></section>}
