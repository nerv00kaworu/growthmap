import {localizedMetadata} from '../../../../content/metadata';import {WorkflowPage} from '../../../../components/WorkflowPage';import {parseLocale} from '../../../../content/i18n';import {notFound} from 'next/navigation';export const generateMetadata=({params}:{params:Promise<{locale:string}>})=>localizedMetadata(params,'docs');export default async function P({params}:{params:Promise<{locale:string}>}){const locale=parseLocale((await params).locale);if(!locale)notFound();return <WorkflowPage locale={locale} page="docs"/>}
