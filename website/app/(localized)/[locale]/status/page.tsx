import {localizedMetadata} from '../../../../content/metadata'; export const generateMetadata=({params}:{params:Promise<{locale:string}>})=>localizedMetadata(params,'status'); import {notFound} from 'next/navigation';import {localizedSecondary} from '../../../../content/secondary';export default async function P({params}:{params:Promise<{locale:string}>}){const c=localizedSecondary((await params).locale,'status');if(!c)notFound();return <section className="page"><p className="eyebrow">{c.eyebrow}</p><h1>{c.title}</h1>{c.body.map(x=><p key={x}>{x}</p>)}</section>}
