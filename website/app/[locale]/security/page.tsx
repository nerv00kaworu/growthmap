import {localizedMetadata} from '../../../content/metadata';export const generateMetadata=({params}:{params:Promise<{locale:string}>})=>localizedMetadata(params,'security');import {notFound} from 'next/navigation';import {parseLocale} from '../../../content/i18n';import {securityProof} from '../../../content/integration-security';import {ProofSections} from '../../../components/ProofSections';export default async function P({params}:{params:Promise<{locale:string}>}){const locale=parseLocale((await params).locale);if(!locale)notFound();const c=securityProof[locale];return <section className="security-page"><div className="page security-hero"><p className="eyebrow">{c.eyebrow}</p><h1>{c.title}</h1><p className="lead">{c.lead}</p></div><ProofSections content={{...c,eyebrow:'',title:'',lead:''}} variant="security"/></section>}
