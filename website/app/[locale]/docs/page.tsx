import {localizedMetadata} from '../../../content/metadata'; export const generateMetadata=({params}:{params:Promise<{locale:string}>})=>localizedMetadata(params,'docs'); import {notFound} from 'next/navigation';import {parseLocale} from '../../../content/i18n';import {Detailed} from '../../../components/ProductContent';export default async function P({params}:{params:Promise<{locale:string}>}){const locale=parseLocale((await params).locale);if(!locale)notFound();return <Detailed locale={locale} page="docs"/>}