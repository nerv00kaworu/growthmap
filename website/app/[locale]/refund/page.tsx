import {localizedMetadata} from '../../../content/metadata';import {notFound} from 'next/navigation';import {parseLocale} from '../../../content/i18n';import {legal} from '../../../content/legal';export const generateMetadata=({params}:{params:Promise<{locale:string}>})=>localizedMetadata(params,'refund');export default async function P({params}:{params:Promise<{locale:string}>}){const locale=parseLocale((await params).locale);if(!locale)notFound();const c=legal[locale].refund;return <section className="page"><p className="eyebrow">{c.eyebrow}</p><h1>{c.title}</h1><p className="lead">{c.lead}</p><p>{c.body}</p></section>}
