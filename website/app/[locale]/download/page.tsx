import {localizedMetadata} from '../../../content/metadata'; export const generateMetadata=({params}:{params:Promise<{locale:string}>})=>localizedMetadata(params,'download'); import {Download} from '../../../components/Localized'; import {parseLocale,type Locale} from '../../../content/i18n'; import {notFound} from 'next/navigation'; export default async function P({params}:{params:Promise<{locale:string}>}){const locale=parseLocale((await params).locale);if(!locale)notFound();return <Download locale={locale}/>}
