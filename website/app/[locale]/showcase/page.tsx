import {localizedMetadata} from '../../../content/metadata'; export const generateMetadata=({params}:{params:Promise<{locale:string}>})=>localizedMetadata(params,'showcase'); import {DemoMap} from '../../../components/DemoMap'; import {parseLocale} from '../../../content/i18n'; import {notFound} from 'next/navigation';import {Detailed} from '../../../components/ProductContent'; export default async function P({params}:{params:Promise<{locale:string}>}){const locale=parseLocale((await params).locale);if(!locale)notFound();return <><DemoMap locale={locale}/><Detailed locale={locale} page="agents" continuation/></>}